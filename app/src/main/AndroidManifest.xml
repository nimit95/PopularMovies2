<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nimitagg.popularmovies2" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetalView"
            android:label="@string/title_activity_detal_view" >
        </activity>
        <activity
            android:name=".reviews"
            android:label="@string/title_activity_reviews" >
        </activity>

        <provider
            android:name=".TestProvider"
            android:authorities="just.some.test_provider.authority" />

        <activity
            android:name=".Fav"
            android:label="@string/title_activity_fav" >
        </activity>
        <activity
            android:name=".Fdetail"
            android:label="@string/title_activity_fdetail" >
        </activity>
    </application>

</manifest>
